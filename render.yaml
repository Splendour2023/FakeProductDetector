services:
  - type: web
    name: FakeProductDetector
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python app.py"
    plan: free
    region: oregon
    branch: main

